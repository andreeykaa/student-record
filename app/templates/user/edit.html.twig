{% extends 'base.html.twig' %}

{% block title %}Редагування користувача{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/user/user.css') }}" rel="stylesheet">
{% endblock %}

{% block page_header %}
    <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3 mb-4">
        <div>
            <h1 class="h2 fw-bold mb-1">Редагування користувача: <strong>{{ user.username }}</strong></h1>
        </div>
    </div>
{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10 col-xl-12">
            <div class="app-card-soft p-4 p-lg-5">
                {{ include('user/_form.html.twig', {'button_label': 'Оновити користувача'}) }}
            </div>
        </div>
    </div>
{% endblock %}
